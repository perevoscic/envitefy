# Envitefy Notes For Future Codex

This is the stuff that was not obvious on first read and is worth keeping in one place.

## Reality Check

- Standing video-provider preference (September 12, 2026, clarified): use OpenAI for generated assets and substantive AI work, and Remotion for animation, editing, composition and rendering. Google/Gemini must not generate video, images or music or process heavy media reviews/transcription. Gemini is allowed only for small, bounded text calls; the studio guard caps these at 4,000 input characters and 256 output tokens on Flash text models, one candidate, no media or tools. Do not batch these into heavy workloads, silently substitute Google, or bypass the provider guard. This clarification supersedes the initial blanket Google ban. The app's separate Calendar, Maps and Vision OCR integrations are outside this video restriction. See `video-studio/STUDIO-GUIDE.md`.

- The repo still has rename drift. `package.json` says `snap-my-date`, and `README.md` still frames the product as OCR-to-calendar first, but the current app is broader: multi-vertical event creation, public event sites, RSVP/sign-up flows, registry links, and admin campaign tooling.
- Anonymous `/` is not the real landing route. `src/middleware.ts` rewrites signed-out users to `/landing`; signed-in users stay on `/`.
- `npm run dev` does not run plain `next dev`. It goes through `scripts/dev-single.js`, defaults to port `3000`, writes to `.next-dev`, and uses `.next-dev.lock` to block a second dev server.
- Dashboard preference (September 7, 2026): keep the original image-led `InvitationEventCard` design for Upcoming Events, including its event details and action buttons. The user prefers these cards to a compact agenda/list; preserve the cards when adding dashboard data or filters.
- Upcoming cards use left-aligned All, My events, and Invited events filters with count bubbles in place of the visible Upcoming Events heading/count. Counts reflect the complete card list before filtering or pagination, excluding the spotlight event.
- Dashboard Drive tile preference (September 7, 2026): show total route distance in miles and drive duration in hours/minutes. Keep departure times, arrival buffers, traffic notes, and drive update timestamps out of this tile.
- Dashboard panel preference (September 7, 2026): keep Guest responses beside Continue creating. Do not restore the large Needs attention panel; use separate compact Schedule conflicts (second summary tile) and Needs attention tiles with clickable details.

## Explicit Draft Saves

- Standing preference (September 8, 2026): keep new event progress in memory until the person explicitly saves. Do not create a draft when an editor opens, after every chat turn, after generating artwork, on pagehide, or when switching tabs.
- Use `UnsavedProgressProvider` for Save and leave, Discard and leave, and Keep editing across Concierge, template editors, signup forms, and manual event creators. Native reload/tab close uses the browser's unsaved-changes warning.
- Discard abandons changes since the last explicit save; it must preserve previously saved drafts. Save failures keep the editor open. Untouched demo content and loading saved content must not trigger a warning.
- Keep publishing separate from saving progress. Preserve the chosen destination after saving, and resume each saved draft in its matching editor with its artwork and form fields.

## Template Gallery Standard

- Football gallery thumbnail preference (September 12, 2026, updated): fill the rounded card interior with artwork and overlay the design name directly inside the image. Keep the white shell, inset and each design's font. No gradient, faded overlay or tinted caption band. Move the name to a clear, readable area of each thumbnail rather than fixing it to the bottom. Prefer quiet edges over the central subject, and choose dark lettering over light artwork and light lettering over dark artwork using the actual crop and wrapped text. This supersedes the earlier caption-gradient treatment and fixed title placement.

- Category page background preference (September 12, 2026): use a soft color suited to each category across the full gallery, header fade, top navigation and safe areas: football light green, gymnastics lavender, birthdays warm yellow, weddings champagne and anniversaries blush, with coordinated pastel colors for the other categories. Use `categoryGalleryPageClassName` and the shared category page palette. This supersedes the ivory-only gallery canvas and top-navigation preference; keep the existing header dimensions, artwork, white template shells and featured landing-page collections.

- Public category gallery top navigation preference (September 12, 2026): remove the purple/blue cloud band behind the logo and menu. Continue the gallery's ivory paper through the top navigation and top safe area, preserving the original compact header dimensions and category artwork below.

- Category header preference (September 12, 2026): each category has its own gallery header, artwork, palette and composition. Football, gymnastics, baby showers and gender reveals use dedicated header components rather than the generic EventDesignGallery header. Preserve the original header height, title sizes and spacing; artwork is an absolutely positioned background and must never add height. Keep themed illustrations on the right, fading into ivory behind the text and the gallery surface below; crop them softly into the upper-right on mobile. Shared decorative primitives are fine, but preserve each category's identity and the wedding runway. Keep featured landing-page collections separate.

- Template gallery navigation preference (September 11, 2026): show a floating up-arrow after scrolling to return to the top. Share it across the automatically loaded galleries, keep it above mobile bottom navigation, respect reduced motion, and scroll the picker's own panel for nested design lists.

- Template list loading preference (September 11, 2026): use `TemplateAutoLoader` to reveal the next batch as the bottom of a category gallery or design picker approaches the viewport. Remove Load more / Show more buttons, reset batching with filters, stop at the full result count, and observe the picker's own scroll panel when nested. Keep the short featured landing-page collections and their Browse all links separate. Browsers without IntersectionObserver show all results so nothing becomes unreachable.
- Category gallery preference (September 11, 2026, expanded): birthdays, weddings, anniversaries, baby showers, gender reveals, gymnastics and public category template galleries use `TemplateMasonryGrid` / `TemplateMasonryCard`. Keep four columns on desktop and two on mobile, stable varied heights per design, and generous gaps; avoid repeating height patterns that align back into rows. Show artwork and the name within the design, without external descriptions, badges or customization text panels. Match the supplied rounded card reference with a softly rounded white shell, a small inset, a fine border and a soft shadow; preserve each design's colors, typography and artwork framing. Keep favorites and selection actions accessible outside the inert artwork. This supersedes the earlier square gallery standard and the initial small-corner, no-inset gymnastics styling for these galleries. The decorative wedding runway and editor color/font swatches are separate.
- Standing preference (September 7, 2026): use the wedding gallery's square thumbnails for all event template lists and design pickers, including birthdays, anniversaries, weddings, baby showers, gender reveals, and gymnastics.
- Reuse `TemplateThumbnailFrame` and `TemplateThumbnailPreview` from `src/components/events/TemplateThumbnail.tsx`: square viewport, 8px white inset, 1.35rem outer corners, 1rem inner corners, warm neutral border, and the shared soft hover shadow. September 11, 2026: every category gallery and design picker uses artwork-focused thumbnails with readable template names and the design's own colors, typefaces, and image framing, like signup forms. Use `TemplateArtworkThumbnail` / `CategoryTemplateThumbnail` with `scaled={false}`; do not shrink full event pages, schedules, or controls into thumbnails. This supersedes the quarter-scale gallery preference.
- Keep thumbnail content inert and hidden from assistive technology; the surrounding link/button supplies the accessible design name and selection action. Keep favorites and selection controls outside the inert preview.
- The decorative wedding hero runway can retain its compact ratio. Full template lists must use the default square preview. Color/font swatches and saved user event cards are separate controls and do not use this gallery standard.
- Signup form thumbnail preference (September 8, 2026): use dedicated artwork-focused thumbnails with readable template names and the design's colors, typography, and photo framing. Keep the shared square frame with `scaled={false}` in the gallery; do not shrink complete signup pages or their form fields into thumbnails.
- Signup form editor preference (September 8, 2026): the design is already chosen in the gallery. On desktop, keep the larger template preview and wizard in the main left column, with customization controls in a separate right sidebar outside the form. Align the sidebar's top edge with the form preview, below the step navigation and introduction. Start the design controls at Color palette, followed by Typography, Header layout, Photos & artwork, and Fine-tune the design. Do not restore the "Make it feel like your event" introduction, "Explore all 150 designs" link, or another theme-selection grid inside the editor.

## Envitefy Brand Name and Pronunciation

- Read [ENVITEFY_BRAND.md](ENVITEFY_BRAND.md) for the mandatory spelling, pronunciation, model instructions, and audio/video quality checks. Visible copy uses **Envitefy**; preserve the locked wordmark below.
- Standing preference (September 9, 2026): match the user's supplied audio reference, approximately **in-VY-tih-fy**, **/ɪnˈvaɪtɪfaɪ/**, as one continuous four-syllable word with VY stress. The recording supersedes the earlier three-syllable en-VITE-fy approximation. Read `ENVITEFY_BRAND.md` for the reference, model direction and required comparison of every spoken occurrence. Visible spelling remains Envitefy.

## Envitefy Logo Brand Lock

The lowercase `envitefy` wordmark is a locked brand asset. Do not change any of the settings below unless the user explicitly asks to change the logo itself.

- Canonical component: `src/components/branding/EnvitefyWordmark.tsx`
- Exact typeface: locally bundled **Josefin Slab**, normal style, weight `700`, optical sizing `auto`
- Font asset: `public/fonts/Josefin_Slab/JosefinSlab-VariableFont_wght.ttf`
- Font registration: the `@font-face` named `"Josefin Slab"` in `src/app/globals.css`
- Exact tracking: `letter-spacing: -0.075em`
- Exact default transform when `scaled` is enabled: `scale-[2.95]`
- Exact internal line height and optical padding: `leading-[1.16] pl-[0.02em] pr-[0.14em] pt-[0.04em] pb-[0.16em]`
- Exact primary fill: `linear-gradient(96deg, #6b3cff 0%, #6757ff 22%, #5a7dff 54%, #37a8ff 100%)`
- Exact light/reversed fill: `#ffffff`
- The text remains lowercase: `envitefy`
- Keep `/fonts/` and the font extensions `woff`, `woff2`, `ttf`, and `otf` excluded from authentication middleware so the real typeface loads instead of falling back to Georgia.
- Placement-specific font sizes may remain responsive, but do not alter the canonical typeface, weight, tracking, scale, line height, padding, colors, gradient stops, or font-loading path without an explicit user request.
- `src/app/landing/page.test.mjs` and `src/middleware.auth-routing.test.mjs` intentionally guard this contract. Treat failures as brand regressions, not tests to weaken casually.

## Generated Image Delivery And Cleanup

Standing user rule (September 7, 2026): after generating new raster artwork for Envitefy, convert it to **WebP with FFmpeg**, then **delete the PNG/JPG/JPEG originals** once the replacements are verified. Complete this workflow in the same task, before reporting the images finished.

1. Encode directly from the generated original using FFmpeg's `libwebp` encoder, with quality `85` and compression level `6` by default. Preserve dimensions, aspect ratio, and transparency unless the user requests a change. Save the final `.webp` in the appropriate project asset directory.
2. Verify that every output is an actual WebP file, decodes successfully, and preserves the intended image quality and dimensions. Update application, template, and manifest references to the final WebP paths and check that those paths exist.
3. After verification, delete the corresponding generated PNG/JPG/JPEG originals, including copies in the generator output directory and temporary or project folders. Delete only the exact originals matched to verified WebP replacements; never use a broad deletion glob. If conversion or verification fails, retain that original until a working replacement is ready.
4. Keep prompt/provenance records and final WebP paths accurate, and confirm that the originals are gone while the WebPs remain intact.

This is standing authorization for that cleanup; do not ask the user again for each generation batch. Follow any later explicit request to retain an original or deliver another format. This rule concerns newly generated artwork, not unrelated existing assets or user-supplied reference files.

## My events vs Invited events (product language)

- **My events** — Events you are **creating and owning** in the workspace. That includes manual creation and upload/snap flows when the result is **not** an “invite card” for the core social-invite verticals (see below). Uploading a schedule, flyer, or anything where you are really **authoring your own event** in Envitefy belongs here, even when the source was a file or camera.
- **Invited events** — Events from **uploading/snapping someone else’s invite** for the classic cases: **birthday party**, **wedding**, **gender reveal** invite (and similar invite-card intent). That is “I received an invite” rather than “I’m hosting / creating my event in the app.”

If you upload something **outside** those invite-card cases, treat it as **My events**: you are still **creating** an event in the product.

**Codebase map (how this ties to data and UI):**

- Left sidebar grouping uses `isInvitedHistoryEvent` in `src/app/left-sidebar.tsx`. It is true when `event_history.data.shared` is set **or** when `isInvitedEventLikeRecord` applies in `src/lib/dashboard-data.ts` (explicit `ownership: "invited"` and/or `invitedFromScan: true`).
- Dashboard/home projections use `normalizeDashboardEventOwnership` in `src/lib/dashboard-data.ts`, which treats `ownership: "invited"` or `invitedFromScan: true` as **invited**.

**Drift:** The main OCR save path `saveToEnvitefyHistory` in `src/components/Dashboard.tsx` still sets `ownership: "invited"` and `invitedFromScan: true` for **all** OCR saves; `src/app/api/google/callback/route.ts` does similar for best-effort history inserts. That does **not** yet match the product definition above until those flags are narrowed to true invite-card verticals only.

## Data Model That Actually Matters

- The real app state lives in Postgres plus JSONB, not Prisma. `src/lib/db.ts` is the primary data layer.
- Prisma is partial and not authoritative for the app shape. `prisma/schema.prisma` only models a subset of `users`, `oauth_tokens`, `password_resets`, and `theme_overrides`.
- The real schema lives in `prisma/manual_sql/init_db_pgcrypto.sql` and other manual SQL files. That is where `event_history`, `event_shares`, `promo_codes`, `email_campaigns`, `rsvp_responses`, and the extra `users` columns are defined.
- Most product behavior hangs off `event_history.data`. When mutating event payloads, preserve the canonical top-level fields that downstream queries assume exist: `startAt`/`startISO`/`start`, `endAt`/`endISO`/`end`, `timezone`/`tz`, `category`, `status`, `ownership`, `createdVia`, and title-like fields. `src/lib/dashboard-data.ts` and the history SQL depend on those fallbacks.
- History and dashboard are separately cached in memory. If a mutation changes what users should see, check both `src/lib/history-cache.ts` and `src/lib/dashboard-cache.ts` for needed invalidation.

## Sharp Edges I Would Want To Remember

- `next.config.ts` sets `typescript.ignoreBuildErrors = true`. `next build` is not a reliable correctness check here.
- Use Biome plus the VS Code diagnostics linter after TS/TSX edits. This repo will otherwise let type errors slip through.
- Avoid using TypeScript `unknown` as an escape hatch. Use concrete types when the contract is known, use `void` for ignored return values, and reserve `unknown` for true untrusted boundaries like parsed JSON, external API payloads, dynamic imports, or generic utility inputs. Narrow `unknown` immediately with guards.
- The codebase is intentionally mixed `ts`, `tsx`, `js`, and `mjs` with `allowJs: true`.
- `SimpleTemplateView.tsx` uses `@ts-nocheck`, so a normal typecheck will not catch missing event action handlers. Run `src/components/SimpleTemplateView.actions.test.cjs` when changing those handlers; it renders the complete event component and exercises Share, beyond the child toolbar tests.
- A lot of regression tests are not behavior tests; they are source-shape guards that assert specific strings or structure. If you refactor intentionally, expect to update the guard tests rather than assuming they are wrong.
- Several core files are huge enough that opening them wholesale is a waste:
  - `src/lib/meet-discovery.ts` is about 10k lines.
  - `src/lib/db.ts` is about 4k lines.
  - `src/app/event/gymnastics/customize/page.tsx` and `src/app/event/dance-ballet/customize/page.tsx` are both about 3.6k lines.
  - `src/app/event/[id]/page.tsx` is about 2.2k lines and acts as a public-render dispatcher.
  Start with `rg` on symbols, then use targeted `sed -n` slices.

## Feature Maps

- Mobile phone preview preference (September 12, 2026): selecting Mobile in `EventPreviewViewport` on a phone fills the available browser width and height at native CSS size, with no scale transform, device-frame padding, rounded frame or shadow. Apply to fullscreen, owner and Concierge event previews; retain device switching and Close without saving or losing in-memory state. Content may scroll underneath the floating controls: do not reserve a toolbar band or inject extra top padding for those controls. Keep the controls readable and tappable, and let pointer gestures pass through the surrounding empty space.
- Past football games preference (September 12, 2026): `FootballSchedule` past-game cards show only matchup, date and supplied score. Show Score unavailable when missing; omit home badges, kickoff time, venues, travel, weather, notes, broadcast, ticket links, source links and calendar actions. Upcoming and undated cards retain their planning details.

- Gymnastics and football section builder preference (September 12, 2026): add and arrange optional sections directly on the event page. Desktop supports dragging from Page sections and reordering by handles; phones use + Add section, a bottom sheet, and Move up / Move down. Keep section editors beside their content and replace duplicate section cards under Your Details with Event settings for design, event basics, access and import. Store versioned `sectionLayout` with explicit saves; Remove hides a section without deleting its fields or responses, with Undo and Restore. Keep the hero and footer anchored. Football shows stacked sections while composing, then real section tabs in clean previews and published pages. Imported gymnastics and football events open the complete category editor so the page and controls share the same in-memory state.

- Template image filter preference (September 12, 2026, corrected): do not add a color filter or Filter switch when the original template artwork does not use one. Football designs display their original artwork colors in the gallery, editor, previews and published pages, including custom image replacements and older saved events with a filter flag. Preserve each design's native text-legibility overlays. This supersedes the blanket automatic tint preference below for these designs.

- Template hero text preference (September 12, 2026): do not put a large standalone heading/copy block above hero artwork. Place stacked headings and captions below the image, or overlay them within the artwork when the design already supports readable overlay text. Apply this across event templates, signup forms, editor previews and published pages, including narrow/mobile layouts. Preserve each design's artwork, palette, fonts, image controls and guest actions; compact navigation is separate.

- Artwork-first hero spacing preference (September 12, 2026): remove blank bands reserved above the hero for Preview, Share and image controls. Float those controls over the artwork, with readable button surfaces and wrapping on narrow screens, so they do not move the image down. Render each action toolbar once.

- Inline text pencil appearance (September 12, 2026): show a plain pencil without a circular border or background in gymnastics and football. Keep the invisible 44px tap target and visible keyboard focus indication.

- Football inline wording preference (September 12, 2026): use the shared FootballPageContent and FootballPageTextProvider in the editor and published page. Pencil controls edit titles, hero captions, descriptions, section headings/captions, navigation, card wording, attendance explanations and schedule link captions directly in memory. Keep title and description in canonical event fields, other wording in normalized footballPageText, and persist only on explicit saves/publishing. Cancel restores the exact prior value; Reset restores inherited template text. Keep pencils outside links/buttons, retain destinations, tab IDs, dynamic counts and calendar actions, and omit editing controls from public/read-only views. Football details and guest-planning notes must also reach the published page.

- Gymnastics inline wording preference (September 12, 2026, expanded): make template welcome copy, captions, section headings, navigation labels, detail labels and body text editable beside the text, as well as the event title and manual description. Include link captions, RSVP field prompts, response choices, submit captions and confirmation messages. Use `InlineEditableText` with accessible pencil, Done, Cancel and Reset controls; pencils must be siblings of links, buttons and form controls, never nested inside them. Store template wording in `gymnasticsPageText`; keep title and description in their canonical event fields. Empty overrides hide decorative text, while missing overrides inherit the current design. Preserve wording through design changes, explicit saves and reloads. Fullscreen previews, gallery thumbnails and published pages have no inline editing controls. Keep the locked Envitefy attribution and guest actions intact.

- Gymnastics editor preview preference (September 12, 2026, updated): desktop uses Preview beside Share and Add to calendar while the edit sidebar is open; hide the redundant pencil. Preview renders the in-memory event directly at the browser's width as a fullscreen page, with one scrollbar at the outside edge. Do not put this preview in an iframe, a scaled device frame or an independently scrolling inner window, and omit the device selector here. Keep app navigation and edit menus hidden, with a reachable Close control. Mobile shows the pencil only while viewing the event, to return to editing; the editor's Preview control opens the same clean preview. Closing preserves unsaved fields, artwork, selected design and editor section without saving or navigating away. This supersedes the shared device-frame preference for the gymnastics editor's fullscreen preview.

- Template image color preference (September 12, 2026, updated): automatically apply a stronger 55% tint from each selected design's own accent to its hero artwork across all event categories and signup forms. Keep a compact Filter on / Filter off switch beside Change image, enabled by default. Preserve the choice in editor memory and through explicit saves, reloads and publishing; changing designs keeps the choice while updating the accent. Use `TemplateImageTone` and the `template-hero-image` class on image elements or artwork-only background layers. Keep text and controls outside the effect, preserve existing template photo treatments and original uploaded files, and do not add an adjustment window or extra save step. This supersedes the earlier subtle automatic-only tint and tint-off default.
- Gymnastics image appearance preference (September 12, 2026, simplified): drag the hero image directly up or down to reposition it, with keyboard access as well. Do not show an adjustment window or position slider. Keep full-image fit as a compact button beside Change image. Preserve the original uploaded file and carry fit and position through explicit saves, reloads and publishing. Change image still opens the system chooser immediately; adjustments do not belong in the sidebar.

- Football schedule layout preference (September 12, 2026): arrange game cards in two equal columns when the schedule has desktop space, and one column on mobile or narrow previews. Use the shared `FootballSchedule` renderer so editor previews and public pages match. Keep a single-game schedule full width.

- Football schedule history preference (September 12, 2026): open Game Schedule on Upcoming, sorted nearest first. Keep completed/past games under Past games and undated fixtures under Date to confirm; never mix undated games into the future list or invent a date. Display supplied dates prominently, normalizing ISO timestamps, US numeric dates and named months using an explicit source season/year when needed. Keep the period tabs local so they do not replace the page's section hash. Raw HTML/JSON parser errors, including messages retained by an already-open editor, must become readable retry guidance.

- Template image replacement preference (September 12, 2026): every template editor's Change image control opens the native system file chooser immediately. Keep these controls on the hero/preview, remove the Images or Photos & artwork sidebar options, and do not insert a custom upload modal or a Use image confirmation step. Reuse `HeroImageEditor`; selected images update editor memory and persist only on explicit save. This supersedes the earlier sidebar Photos & artwork placement for signup forms.

- Mobile event owner toolbar preference (September 12, 2026): place the Edit pencil in the floating event actions bar beside Share and Delete, with an icon-only 44px target on phones. Do not register an owner-view Edit action in the top navigation. Preserve the existing edit-menu URL and guarded return flow. Templates that already supply their own mobile Edit beside guest actions retain that placement. This supersedes the September 11 owner-view mobile top-navbar pencil preference.

- Football custom URL preference (September 12, 2026): expose Custom URL in the football editor, with a full `/event/` link preview and read-only availability checks. Single-game suggestions use mascot matchups and the year (for example `seahawks-at-vikings-2026`); season schedules use the team and season. Keep edits in memory and in explicit draft snapshots until publishing. Publish reserves the exact requested address atomically, rejects collisions, and preserves previous published addresses as aliases. Keep school identity and canonical event IDs unchanged.

- Football game travel preference (September 12, 2026): fill missing stadiums and ticket portals from verified public hosting-school sources, with Find tickets, stadiums & miles available in the schedule preview and editor. Keep manual edits and explicit saves. Use Google Routes first and compare viable road alternatives; Mapbox's first result can favor a much longer interstate detour. South Walton to Gulf Breeze is about 62 miles via US-98, not the 108-mile I-10 alternative. Display the road name beside the mileage, use the same stadium addresses in directions, and never reuse a random event's ticket link for every fixture. Label school-wide ticket portals clearly; never use Google search as the ticket-button destination. Verified ticket buttons open the hosting school’s Hudl, GoFan or other official sales page. Return stadium/ticket facts before slower route and web lookups, retain successful batches after partial failures, and handle HTML/API errors without exposing raw JSON parser exceptions.

- Football section preference (September 12, 2026): Details, Game Schedule, Updates and other populated football sections are real accessible tabs showing one panel at a time in editor previews and public pages. Keep section hash links, keyboard navigation and in-memory editor state. Use `FootballSectionTabs` and derive tabs from populated sections. South Walton's confirmed team name is **South Walton Seahawks**; use it in team details and generic football headings even when a source names only South Walton High School. Game headings use short mascot names, such as **Seahawks at Vikings** for South Walton at Fort Walton Beach, with the hosting mascot in the Home badge. Do not repeat the full school matchup underneath. Keep stadium names visible, hide street addresses from game cards, and show Get directions only for away games. Preserve full school identity for calendar titles, ticket-provider lookup and stadium lookup, and retain addresses internally for routes and calendar locations. Gulf Breeze’s confirmed mascot is Dolphins. Other mascots must come from the source; retain the school name when unknown.

- Football import preference (September 12, 2026): Upload / URL & Prefill reads a file or public website into the current editor through `/api/football/prefill`, using OpenAI and keeping the selected template and hero. Do not create a discovery shell, upload an asset, navigate to a different template, or save a draft during prefill. Missing source facts replace demo values with blanks; never fill them from Chicago/Panthers samples. Render only populated sections. Games name both teams, identify the home team, and use each away game's own venue; optional road mileage starts at the home stadium address. `FootballSchedule` shares game directions, calendar, tickets and available travel/weather details between the editor and published football page. Category hero editors use `HeroImageEditor` for a visible replacement control, keeping new images in memory until explicit save.

- Dark-template sidebar preference (September 12, 2026): increase the sidebar's light frost and purple-label contrast only when the registered event background is dark. Keep the existing transparency on light templates, selected pills, branding, and desktop/mobile blur strengths. Derive tone from the shared event color registration without new observers, image sampling or requests, and restore it on navigation.

- Event canvas standard (September 11, 2026): the rendered event's own background color fills the surrounding canvas, page gutters, navigation and safe areas across public events and creation previews. Use `EventCanvas` with the shared event color registration; read the actual rendered page so changing the selected design updates the color. Keep template artwork and section surfaces intact, exclude thumbnails and editor controls, and restore app chrome when leaving the event.

- Gymnastics footer preference (September 11, 2026): footer copy, links and social icons inherit each template's foreground text color, including on dark backgrounds. Use the shared branding and social links' `inheritColor` option; preserve the canonical Envitefy wordmark.

- Event guest action placement (September 11, 2026): place the gymnastics event's Share and Add to calendar controls above the hero on desktop and mobile. Below 640px show icons only, with 44px touch targets and accessible action names; retain desktop labels and the selected calendar provider's accessible name. In mobile gymnastics owner views and editor previews, put the Edit pencil immediately to the right of Add to calendar in this same row, with a matching 44px target. This supersedes the top-navbar pencil placement for gymnastics; keep the existing edit menu and protected return navigation.

- Calendar action preference (September 11, 2026): event Calendar/Save to Calendar buttons say **Add to calendar** and open the shared Google, Apple, and Outlook chooser. When a valid default is selected, say **Add to Google Calendar**, **Add to Apple Calendar**, or **Add to Outlook Calendar** and open that provider directly. Reuse `CalendarAction` / `useCalendarAction` across event templates, scanned invitations, and Live Cards. Signed-in defaults come from the current account profile and must have a verified connection; never guess from the device or borrow another account's browser preference. Settings changes notify mounted controls, including preview iframes.

- Preview hydration constraint (September 11, 2026): navigation mounting does not mean nested event content has hydrated; dynamic renderers can remain inside pending Suspense boundaries. Keep preview background overrides in an iframe stylesheet using structural selectors. Do not add background-marker attributes to React-rendered event surfaces or artwork, and do not suppress hydration warnings to hide the mismatch.

- Saved event Cancel preference (September 11, 2026): Cancel in the gymnastics editor returns to the original event URL with `tab=event`, falling back to the saved event ID for older editor links. Preserve unsaved-change protection, including embedded editors; do not send saved events back to `/event/gymnastics`.

- Desktop event toolbar preference (September 11, 2026): center the device selector and the Edit, Share, and Delete actions together as one floating group over the event canvas beside the sidebar. Do not center only the device selector while the action group extends to its right.

- Event chrome preference (September 11, 2026): the actual event design's background color must fill the top navigation area, page inset, and mobile safe areas in both the owner view and edit screen, including after returning from Edit. Use the shared event page color registration; never restore app lavender clouds behind event navigation. Mobile Edit is a pencil in the top navbar; desktop Edit is a pencil beside the floating event controls. Remove the purple bottom-right Edit trigger when the mobile navbar is available. The pencil opens the edit menu directly, and Back to preview returns to the owner event view with its device and sharing controls, preserving unsaved-change protection. This supersedes the earlier bottom-right Edit preference.

- Live Card preview layout preference (September 10, 2026, updated): center the card in 90% of the available screen height, leaving 5% above and below. Place Share inside the upper-left corner and X Close inside the upper-right corner on mobile/Safari and desktop; keep both controls inside the card with 44px touch targets. This supersedes the earlier 80% height, adjacent top-right controls and Close placement below the card. Respect safe areas. Scale the artwork proportionally to cover the frame from top to bottom, cropping the sides as needed on narrow phones; do not add blurred filler bands. Keep guest actions inside the card.

- Concierge sidebar icon preference (September 10, 2026): use `public/brand/concierge-chat.webp`, the tightly trimmed transparent version of the supplied `public/brand/concierge-chat.png`, for the Envitefy Concierge navigation item. Keep the supplied purple e/chat-bubble shape, existing active/hover treatment and accessible navigation label.

- Live Card owner-view preference (September 10, 2026, clarified): saved Live Cards and flyers/invitations open directly in the existing Design editor, including sidebar, dashboard and card-share owner entry points. Only event pages open in the full event/device view. Card and flyer previews use a bounded artwork dialog with Close and their existing guest actions, without Desktop, Tablet or Mobile buttons, an iframe device frame, or fullscreen navigation suppression. Apply the same rule in Concierge. Keep the collapsible sidebar and explicit artwork saves. This supersedes the earlier standalone owner card view and shared device controls for cards.

- Sidebar expansion and branding preference (September 10, 2026): use the supplied `public/brand/e-only.png` for the collapsed desktop icon rail and `public/brand/envitefy-wordmark.png` when expanded, including the mobile drawer/header. The transparent e-only icon supersedes `public/brand/logo-e.png`. This is the explicitly requested sidebar branding; leave the canonical wordmark component unchanged for other placements. Desktop navigation expands on mouse hover or keyboard focus, with a separately persisted pin control to the right of the expanded wordmark. Keep that control icon-only, with an accessible label and no visible words. Root navigation and category menus share `nav-chrome-menu-label`: the existing category menu's inherited app font, bold uppercase at 0.95rem with 0.13em tracking, and matching lavender text colors. Keep the desktop content canvas at its expanded width while the outer rail animates, and hide inactive panels immediately so labels never squeeze or overlap on hover. Keep mobile drawer state independent, retain titled scrolling panels and profile actions, and match the main content inset to the current sidebar width.

- Event opening preference (September 10, 2026): clicking an owned event page in My Events opens `?tab=event` and `EventOwnerView`, filling the area beside the collapsible sidebar, with Edit, Share and Delete beside the device selector. Default to Desktop on desktop browsers, Mobile on phones, and Tablet on tablet-sized browsers, using the browser viewport rather than a narrow preview container. Remove the redundant Manage your event / Your event page editing panel; Edit goes to the existing event editor. After successful deletion, clear the selected event and return to the main dashboard with My Events open instead of selecting another event. Preserve guest-response dashboards, actual Live Card artwork editing, and explicit saves.

- Event preview device preference (September 10, 2026): use the shared `EventPreviewViewport` for event-page previews, including Concierge event pages. Show icon buttons for Desktop, iPad/tablet, and Mobile with a selected state, plus Close. Resize a real iframe viewport so breakpoints and fixed artwork match the selected device; scale it to fit the available screen. Keep embedded owner controls out of previews; the owner event view supplies Edit, Share and Delete in its outer toolbar. Live Cards and flyers/invitations use artwork previews without device controls. Keep unsaved preview artwork in memory.
- Fullscreen preview navigation preference (September 10, 2026): explicit fullscreen event-page previews hide desktop sidebar and mobile navigation and remove their reserved spacing. Use the shared preview chrome suppression, restore navigation when the last event-page preview closes, and leave the inline event-workspace preview and unsaved editor state intact. Default owner event views retain the sidebar; card/flyer artwork dialogs do not suppress navigation.
- Preview background preference (September 10, 2026): extend the rendered event's own background across the full preview canvas, including the space around tablet and mobile views. Keep the device toolbar transparent, without a white strip or pink/lavender workspace fill. Preserve the device icons and Close control, with colors readable against the event theme.
- Preview background continuity (September 10, 2026): paint image/gradient page backgrounds once across the outer preview canvas. Make the matching iframe page surfaces transparent and hide only the copied decorative artwork layer, so backgrounds do not restart at the iframe edges. Preserve hero images, detail-card surfaces, real device breakpoints, and the original backgrounds outside previews.
- Preview title preference (September 10, 2026): omit the duplicate event title from the top-left preview toolbar. Keep the title in the event content and accessible preview labels, and keep the device selector centered.

- Sidebar event-row action preference (September 10, 2026): hide Share and Delete/Remove controls from My Events and Invited Events rows, including past events, on desktop and mobile. Remove the action container entirely so it reserves no title space, even on inactive rows. Let titles extend to the row's right padding while preserving category icons, dates, pending badges and full-card press feedback. Event-page actions are separate.

- Scanned flyer preservation preference (September 10, 2026): designed flyers, posters and invitations stay in the hero during and after artwork generation; generate only a coordinated background for them. Do not offer to replace a designed flyer with a generated hero. Older or unclassified scans must also keep the original, even if a previous request stored scanHeroMode generated; only recognized paperwork may generate a replacement hero. Plain paperwork, appointment slips and business/contact cards use generated heroes and keep their source in Original document, regardless of an older manual hero choice. Remove the Use original artwork / Use generated artwork switch buttons; the source type decides the hero. Hide the separate tile only when a supported hero actually displays the original, and open the exact-file popup from that hero. Preserve a tile fallback for missing hero images/PDFs and owner-only medical access. Keep Good to know for useful extras, including printed reception plans such as “Dinner and dancing to follow” and “Reception to follow”, preparation, access and restrictions. Omit generic reminders and repeated logistics. The latest request explicitly restores reception wording and supersedes the earlier request to hide it. This supersedes the earlier optional generated-hero choice for designed invitations.

- Sidebar scrolling preference (September 10, 2026): use `SidebarListPanel` for every titled sidebar list, including Admin, Create Event, Other Events, Concierge, My Events, and Invited Events. Keep Back and the title outside the scrolling list viewport so rows clip below the header. Keep the header on the shared sidebar surface; do not restore a separate header fill or backdrop blur.

- Mobile sidebar appearance preference (September 10, 2026): keep the mobile navigation and My Events list predominantly white with strong translucent frost and only faint blurred artwork, even over dark cards. Use the sidebar's own white backdrop so a dark page scrim cannot show through the drawer. Preserve the desktop sidebar appearance and keep nested navigation panels transparent.

- Mobile edge colors (September 10, 2026): use soft, overlapping violet, blue and cyan radial clouds inspired by the locked wordmark at the top and bottom of mobile page/safe-area surfaces, including Safari, Chrome and installed apps. Keep the sidebar center predominantly white and desktop surfaces unchanged. Native browser/OS bars that accept only a single color use the matching periwinkle tint; never put a CSS gradient into theme-color metadata or the web app manifest.

- Original-document popup preference (September 10, 2026): fit the popup to the displayed image's dimensions, constrained to the viewport. Keep Share, Download and Close floating over the upper-right of the image/file, with readable translucent controls and no separate header or padded frame. PDFs fill their viewing area. Preserve the accessible dialog title, keyboard dismissal and exact-file actions.

- Parallel scan artwork preference (September 10, 2026): start hero/background generation from the first reliable event brief while schedules, location and remaining scan work continue. Keep unsaved artwork in memory; both save routes redeem the owner/brief-bound signed ticket and reuse the running job. No event draft or generated upload before explicit save. Retain designed invitations, WebP conversion, owner-only originals, claim protection and expired-job retries. See `docs/snap-personalization-2026-09-10.md`.

- Sidebar event-list preference (September 10, 2026): remove category headings and show one chronological upcoming list from nearest to latest across all categories. Use each event's category icon (stethoscope for medical appointments, rings for weddings, cake for birthdays) in place of the generic calendar. Add a subtle divider and tiny month/year label as the month changes. Keep undated events last, labelled **Draft** both in the divider and beneath each title instead of No date; this is sidebar display copy and does not change saved publishing status. Keep past events behind the existing toggle, most recent first. Preserve category information for accessibility and draft status beside the date.

- Medical contact-card preference (September 10, 2026): show the printed clinic **Phone** and **Fax** together in one **Phone & Fax** tile, with individually labelled values and Phone first. Use a phone icon for the combined tile; keep a printer for Fax-only tiles. Preserve both as distinct source facts, even if they share a number. Phone facts must not be removed as already-rendered contact shortcuts. Recover missing labelled contacts from saved OCR text for older appointments without exposing the full transcript or altering saved event URLs.

- Scanned detail-grid preference (September 10, 2026): use two columns on phones as well as desktop, with equal-height tiles. On phones, place the label and 40px icon above the full-width value so names and provider details wrap readably. Original document remains the final tile. Keep the popup toolbar aligned right even when it wraps. Prepare the viewing copy as its tile approaches the viewport, then retain the exact original File for Save, Download and Share while the event remains mounted; release both on navigation and preserve authenticated delivery.

- Scanned document optimization preference (September 10, 2026): after an explicit scan save, use FFmpeg in the background to prepare a smaller WebP viewing copy of JPEG, PNG or full-resolution WebP uploads. Normalize phone orientation, contain the viewing copy within 2400px without enlarging, use libwebp quality 85/compression 6, and retain it only when smaller. Encrypt the derivative and serve it through the authenticated original endpoint; preserve the exact uploaded source for Save, Download and Share. Durable owner/source claims prevent duplicate or stale work; older saved images and interrupted jobs resume on owner viewing requests. PDFs keep their original viewer.

- Original-file caching preference (September 10, 2026): reuse stored original bytes in a five-minute server-memory cache, limited to 32 MiB and 32 entries per process, with concurrent requests combined. Every request must still resolve the current event and session before accessing the cache. Keep encrypted originals encrypted in that cache and decrypt only after authorization; retain browser `private, no-store` delivery. Source URL changes select a fresh cache entry, and failed storage reads are retried rather than cached.

- Scan artwork preview navigation (September 10, 2026): `/event/scan-artwork-preview` registers its saved event URL with `EventSidebarRoute`, so My Events opens and the matching appointment is active. Keep this association scoped to the mounted route; do not hardcode preview patient IDs or slugs in the shared sidebar or change the browser URL.

- Scanned detail-icon preference (September 10, 2026): keep right-side icons visible on mobile and desktop, using 40px circles on phones and 48px circles from the small breakpoint. Medical tiles use a person for Patient, an ID card for Patient ID, a stethoscope for Clinician, a printer for Fax, a building for Appointment provider, and a document for Original document. Keep decorative icons hidden from assistive technology and preserve room for text and View/Save controls.

- Scanned mobile typography preference (September 10, 2026): use 18px text with a snug line height for the date, time, venue and address values in the shared scanned-event details card. Keep desktop values at 30px; long mobile addresses should remain readable without the oversized wrapping.

- Scanned background scrolling preference (September 10, 2026): keep generated page artwork fixed to the viewport while the details, hero card and original-document section scroll. Fill the visible event area, respect the desktop sidebar offset, and avoid the old blank seam below the artwork. Use a fixed decorative layer rather than mobile-dependent `background-attachment: fixed`.

- Original-document tile preference (September 10, 2026): show **Original document** as the final detail tile above the **Snapped by Envitefy** footer. Keep only an **eye icon** for View and a **download icon** for Save, with no visible button words. Preserve the Original document heading, accessible action labels and 44px touch targets. View opens an in-page document popup with a dimmed/blurred backdrop and **Share**, **Download** and **Close** controls, never a new browser tab. Share the actual file through the device share chooser when supported; preserve owner-only access rather than publishing a medical source URL. Do not show a collapsible section, inline preview/thumbnail, filename or helper paragraph in the tile. This supersedes the earlier thumbnail/disclosure and new-tab preferences. Preserve the exact original for saving.

- Scan media preference (September 10, 2026): appointment slips, schedules and paperwork use a unique generated hero plus coordinated background. Designed invitations/flyers retain the original hero by default, with an owner choice to generate artwork. Preserve the exact uploaded original in an **Original document** tile with **View** and **Save**. Medical originals are owner-only; new medical uploads use encrypted storage and authenticated delivery, while the legacy blob proxy also checks medical source ownership. Generated artwork contains no identifying document data. See `docs/snap-personalization-2026-09-10.md`.

- Appointment category preference (September 10, 2026): use the existing **Medical Appointments** category for medically supported scans, including ENT visits, and keep nonmedical appointments under **Appointments**. Apply the same category and concise patient title in the sidebar, dashboard and scanned event page; preserve existing event URLs.

- Snap personalization preference (September 10, 2026): use a concise person/event title for scanned appointments, for example first name + ENT appointment, rather than clinic shorthand such as Estab Pt. Generate a unique event/person-aware background after a signed-in scan is saved. Use a clearly supported age at the event date for age-appropriate artwork, keep identifying details out of image prompts, and leave unclear ages/people unknown. Keep printed Patient, Patient ID, individual clinician, Fax and clinic/organization details in the appointment view. Medical cards label the person **Clinician** and the organization **Appointment provider**, never **Host**; retain the stored Appointment Provider/Host facts for compatibility and preserve printed credentials without assuming the person is a doctor. Hide DOB/birthday cards and omit the generic “Bring your appointment details with you.” Good to Know reminder. See `docs/snap-personalization-2026-09-10.md` for the extraction, background generation and retry paths.

- Chat product selector preference (September 8, 2026): keep the Live Card, Flyer/Invitation, and Event Page choices on the empty starting screen only. Do not render the old product navigation bar between conversation messages, including when a draft has no requested output.

- Live Card button placement (September 8, 2026): keep RSVP, Overview, Location, Calendar, and other guest action buttons overlaid inside the bottom of the artwork, as in the original design. Do not move them into a separate row below the image or add a solid black footer. Continue the artwork behind the controls; keep essential lettering and faces clear of the button area.

- Concierge V2 was fully removed on September 6, 2026 at the user's request, including its stored records. The current creator is `/chat` (`ConciergeChatClient` -> `ChatProductPreview`, `/api/creation/intake` -> `src/lib/concierge/intake.ts`); published Live Cards use `/card/[id]`. All `/concierge-v2` page paths redirect to `/chat`. Do not restore V2 drafts, APIs, tools, flags, seed scripts, or schema setup. The shared `event-website-schedule.ts` preserves current event schedules independently. `/api/concierge/message` and `/api/concierge/events/[id]/message` belong to the current creator and remain active. See `docs/concierge-route-map.md` for the deletion scope. Verify the full `/chat` UI, not only an isolated preview fixture.
- Weddings are split between static theme data and renderer code.
  - Catalog and metadata: `templates/weddings/index.json`
  - Per-theme configs/assets: `templates/weddings/*/config.json`
  - Public renderer implementations: `src/app/event/weddings/_renderers/*`
  Adding or changing a wedding theme usually means touching both config/assets and a renderer path.
- Gym meet discovery is its own subsystem, not a minor event variant.
  - Parse entry: `src/app/api/parse/[eventId]/route.ts`
  - Enrichment follow-up: `src/app/api/parse/[eventId]/enrich/route.ts`
  - Core logic: `src/lib/meet-discovery.ts`
  - Builder status logic: `src/lib/meet-discovery/status.ts`
  - Template registry: `src/components/gym-meet-templates/registry.ts`
- Football discovery has a parallel pipeline with separate normalization and rendering. Look at `src/lib/football-discovery.ts`, `src/components/football-discovery/*`, and `src/app/event/football-season/customize/*`.
- Smart signup is effectively a second product surface.
  - Builder/viewer: `src/components/smart-signup-form/*`
  - Public page: `src/app/smart-signup-form/[id]/page.tsx`
  - Template image manifest and assets: `public/templates/signup/manifest.json`, `src/assets/signup-templates.ts`

## Route And Runtime Quirks

- Calendar notice preference (September 10, 2026): show calendar sync progress/results as compact bottom toasts, never large top banners. Use concise copy, a small Settings/Connect action and a 44px dismiss control. Auto-dismiss success/progress after five seconds and errors/setup after eight; pause while hovered or keyboard-focused, and allow the final result after progress is dismissed. Preserve calendar syncing and the existing setup dialog.

- Calendar description preference (September 10, 2026): use the shared `src/lib/calendar-description.ts` formatter for calendar sync, manual calendar links and ICS exports. Group available event details, contacts, RSVP, notes and links with labels and blank lines; preserve authored paragraphs. Medical saves use Patient, Patient ID, Clinician and Appointment provider facts, with Phone before Fax, instead of the flattened source transcript or DOB. Keep formatting idempotent across adapters and the saved event link intact at the end.
- Calendar formatter regression guard (September 10, 2026): `buildCalendarDescription` assembles and sanitizes the description, then `compactCalendarDescription` removes repeated primary event fields. Preserve both functions; compaction alone cannot format a raw scan. Pass the saved `details` and `eventUrl` through `buildCalendarLinks`. Outlook descriptions use plain-text content so line breaks survive. `src/lib/calendar-description.integration.test.mjs` exercises automatic sync, provider adapters, manual links and actual single/bulk ICS downloads together.

- Scan calendar timing preference (September 10, 2026): save the pending calendar sync with the scanned event and run provider work after the history response. Open the event immediately after saving; never await calendar sync in Dashboard before navigation. Keep Google/Outlook enabled, preserve duplicate prevention, and show owner-only sync status on the event page. Pending or interrupted work resumes through authenticated status reads.

- Standing preference (September 5, 2026): Google/Outlook calendar syncing must remain enabled. Keep `CONNECTED_CALENDAR_SYNC_ENABLED = true` in `src/config/calendar-sync.ts`; do not pause it for verification or maintenance unless the user explicitly requests that change. The earlier verification pause is superseded; see `docs/calendar-sync-pause-2026-09-05.md`.
- Public access is controlled by middleware heuristics, not just folder names. `src/middleware.ts` treats `/event/[slug-or-id]` and `/smart-signup-form/[id]` as public share pages.
- `/event` itself is different from `/event/[id]`; the base route requires a session cookie and behaves like app workspace entry, not a public share page.
- Event passcodes are implemented with per-event cookies in `src/lib/event-access.ts`. If you change access-code behavior, also inspect:
  - `src/app/api/events/[id]/unlock/route.ts`
  - `src/app/api/rsvp/attendance/route.ts`
  - `src/app/event/[id]/page.tsx`
- Most API routes explicitly pin `runtime = "nodejs"`, which is important because OCR, PDF parsing, Sharp, Google APIs, and raw Postgres all assume Node.
- The notable edge-runtime exceptions are `src/app/api/maps/static/route.ts`, `src/app/api/templates/signup/route.ts`, and `src/app/event/[id]/opengraph-image.tsx`. Do not pull Node-only imports into those by accident.

## Testing And Validation

- There is no useful top-level `npm test` flow right now.
- The targeted test command is usually `node --test path/to/file.test.mjs`.
- There are also `.test.ts` files, but many of the active regression guards are `.test.mjs` source inspections.
- Good default validation sequence after edits:
  - `npm run lint -- <paths>` if the change is broad enough to justify it.
  - `node --test <targeted-tests>`
  - `npm run lint:vscode -- <touched files>`; this wrapper resolves the installed Chat to CLI extension and provider bridge instead of relying on a versioned extension path.

## Docs Drift

- Trust the route tree, registries, and SQL more than the top-level docs. The README is directionally useful, but it undersells how much of the app now lives in wedding themes, discovery workflows, public event rendering, signup tooling, and admin operations.

## Product Marketing Catalog Contract

- `src/lib/product-marketing-catalog.ts` is the customer-facing product and sales source of truth. The admin email generator imports it directly into the email marketing LLM prompt.
- Every launched customer-facing feature must add or update its catalog entry in the same change. Include the customer promise, concrete proof points, availability (`core`, `event-dependent`, or `specialized`), and when the marketing team should sell it.
- Do not duplicate the complete feature inventory inside `src/lib/admin/email-generation-guide.ts`; keep that file focused on campaign behavior and let it consume the shared catalog so future feature updates reach email generation automatically.
- Do not add admin-only, disabled, feature-flagged, experimental, or unverified capabilities to the marketing catalog.

## Envitefy Video Studio

- The admin Content Studio at `/admin/marketing-images` was retired on September 5, 2026. Its dedicated APIs and Vercel reconciliation cron are removed; do not reintroduce them for local video production. See `docs/admin-content-studio.md`. The customer `/studio` entry was retired on September 7, 2026: it redirects to the public `/envitefy-concierge` explainer, whose creation and authentication actions lead to `/chat`. Live Card attribution links use `/envitefy-concierge`. Keep shared Studio-named renderers/assets used by current cards; the old URL is not the active creator.
- Video production lives in `video-studio/`. For video creation, revisions, aspect-ratio adaptations, or copy accompanying a studio video, read `video-studio/AGENTS.md` and use `video-studio/.agents/skills/envitefy-video-producer/SKILL.md`.
- Ad concepts, scripts, storyboards, visual direction, and campaign copy use `video-studio/.agents/skills/envitefy-ad-creative/SKILL.md`. The producer uses it for new campaigns and substantial story revisions, then continues production. Ideas-only requests stay within written creative work; simple production edits reuse the saved plan.
- Standing video preferences are in `video-studio/STUDIO-GUIDE.md`; campaign history is indexed in `video-studio/projects/README.md`. Update these records when relevant feedback arrives so fresh tasks do not need the conversation repeated.
