import fs from 'node:fs/promises';import dotenv from 'dotenv';
dotenv.config({path:'../.env',quiet:true});dotenv.config({path:'../.env.local',override:true,quiet:true});
const data=await fs.readFile('out/john-space-disco/vertical-v3-final-audio.mp3');
const response=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent',{method:'POST',headers:{'x-goog-api-key':process.env.GEMINI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{inlineData:{mimeType:'audio/mpeg',data:data.toString('base64')}},{text:'Listen to this completed 30-second advertisement. Transcribe every audible spoken line. Describe how the brand name in the Concierge narration sounds, including whether it is one connected word or has unnatural pauses. Are all words complete and intelligible over the music? Check the final child line for cutoff. Identify actual audible defects only. Return concise valid JSON without markdown fences.'}]}],generationConfig:{temperature:0.1}}),signal:AbortSignal.timeout(60000)});
if(!response.ok)throw new Error('Review HTTP '+response.status);const j=await response.json();const t=j.candidates[0].content.parts.map(p=>p.text||'').join('\n');await fs.writeFile('projects/john-space-disco/vertical-v3-audio-review.json',t);console.log(t);
