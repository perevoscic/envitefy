import fs from 'node:fs';
import {spawnSync,execFileSync} from 'node:child_process';
const out='out/john-space-disco/',project='projects/john-space-disco/';
const file=out+'john-space-disco-16x9-v5.mp4';
const production=JSON.parse(fs.readFileSync(project+'wide-v5-voice-production.json','utf8'));
const expected='<phoneme alphabet="ipa" ph="ɪnˈvaɪtiˌfaɪ">Envitefy</phoneme>';
if(production.ipa!=='ɪnˈvaɪtiˌfaɪ'||production.ssml!==expected)throw Error('The submitted IPA must exactly match the user request');
const probe=JSON.parse(execFileSync('ffprobe',['-v','error','-count_frames','-show_entries','stream=codec_name,width,height,r_frame_rate,nb_read_frames,sample_rate,channels:format=duration,size','-of','json',file],{encoding:'utf8'}));
const video=probe.streams.find(s=>s.width),audio=probe.streams.find(s=>s.channels);
if(video.width!==1920||video.height!==1080||video.r_frame_rate!=='30/1'||Number(video.nb_read_frames)!==900||Number(probe.format.duration)!==30||audio.channels!==2)throw Error('Unexpected V5 format');
const decode=spawnSync('ffmpeg',['-v','error','-xerror','-i',file,'-f','null','-'],{encoding:'utf8',windowsHide:true});
if(decode.status)throw Error(decode.stderr);
function streamHash(name){return execFileSync('ffmpeg',['-v','error','-i',name,'-map','0:v:0','-c:v','copy','-f','hash','-hash','sha256','-'],{encoding:'utf8'}).trim();}
const originalHash=streamHash(production.baseVideo),revisedHash=streamHash(file);
if(originalHash!==revisedHash)throw Error('Video stream unexpectedly changed');
const levels=spawnSync('ffmpeg',['-hide_banner','-i',file,'-vn','-af','ebur128=peak=true','-f','null','-'],{encoding:'utf8',windowsHide:true});
fs.writeFileSync(out+'wide-v5-loudness.txt',levels.stderr);
const report={probe,fullDecodePassed:true,videoStreamUnchanged:true,videoStreamHash:revisedHash,visualReviewInheritedFrom:project+'wide-export-checks-v3.json',pronunciation:production,pronunciationCheck:'Exact IPA/SSML verified in completed provider request. Provider normalized alignment uses unstressed CMU token labels for this IPA input, so it is not treated as an acoustic stress assessment.',loudnessSummary:levels.stderr.slice(levels.stderr.lastIndexOf('Summary:')),localTranscript:project+'wide-v5-final-voice-review-local-transcript.json'};
fs.writeFileSync(project+'wide-export-checks-v5.json',JSON.stringify(report,null,2)+'\n');
console.log(JSON.stringify(report,null,2));
