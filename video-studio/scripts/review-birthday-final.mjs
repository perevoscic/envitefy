import fs from 'node:fs/promises';import dotenv from 'dotenv';
dotenv.config({path:'../.env',quiet:true});dotenv.config({path:'../.env.local',override:true,quiet:true});
const data=await fs.readFile(process.argv[2]||'out/birthday-second-job-qa.mp4');
const prompt='Review this finished 30-second vertical TikTok in detail. Transcribe ALL audible spoken dialogue verbatim with start and end times. Assess whether the dialogue is natural, intelligible and not overwhelmed by the music. Describe the music and any unwanted vocals. Inspect readable captions, both RSVP and calendar action sequences and visible confirmations, cut transitions, any grey padding or white border artifacts, distracting anatomy defects, consistency of parent and setting. Confirm final CTA text. Return concise JSON with dialogue, audio, productDemo, visualIssues, cta, overallPass. Do not assume anything beyond the attached video.';
const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent',{method:'POST',headers:{'x-goog-api-key':process.env.GEMINI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{inlineData:{mimeType:'video/mp4',data:data.toString('base64')}},{text:prompt}]}],generationConfig:{temperature:0.1}}),signal:AbortSignal.timeout(60000)});
if(!r.ok)throw new Error('Review HTTP '+r.status);const json=await r.json();const result=json.candidates[0].content.parts.map(p=>p.text||'').join('\n');await fs.writeFile(process.argv[3]||'projects/birthday-second-job/final-media-review.json',result);console.log(result);

