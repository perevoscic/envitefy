import fs from 'node:fs/promises';import dotenv from 'dotenv';
dotenv.config({path:'../.env',quiet:true});dotenv.config({path:'../.env.local',override:true,quiet:true});
const data=await fs.readFile('projects/wedding-200-texts/final-review.mp4');
const response=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent',{method:'POST',headers:{'x-goog-api-key':process.env.GEMINI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{inlineData:{mimeType:'video/mp4',data:data.toString('base64')}},{text:'Audit this finished 25-second wedding advertisement. Transcribe exactly all SPOKEN dialogue (exclude written captions) and report whether any speech is repeated or cut short. Is music playful and speech intelligible? Does music stop after the final spoken line? Check that couple clinks and lowers glasses, RSVP confirmation and calendar controls are visible, final URL is envitefy.com, text fits screen, no obvious distorted props or faces. Note actual problems, not hypothetical ones. Distinguish on-screen text from speech. Return concise JSON with spokenDialogue,audio,visuals,issues.'}]}],generationConfig:{temperature:0.1}}),signal:AbortSignal.timeout(60000)});
if(!response.ok)throw new Error('Review HTTP '+response.status);const j=await response.json();const text=j.candidates[0].content.parts.map(p=>p.text||'').join('\n');await fs.writeFile('projects/wedding-200-texts/final-media-review.json',text);console.log(text);
