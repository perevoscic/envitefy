import fs from 'node:fs/promises';
let script=await fs.readFile('scripts/generate-fridge-revision.mjs','utf8');
script=script.replace("if(!shots[name])throw Error('Unknown shot');", "if(!shots[name])throw Error('Unknown shot');");
script=script.replace('async function read(file)',`shots['v3-insert-camera']='Create a six-second photorealistic live-action vertical 9:16 extreme close-up for a warm family kitchen advertisement. One black smartphone held upright in portrait by a mothers two medium-brown hands with mustard cardigan cuffs. The phone is square-on to the cinema camera, with NO perspective tilt, centered and filling 83 percent of frame width and 84 percent of frame height; full black rounded bezel is visible with small margins. Phone remains perfectly still at those coordinates for the entire clip. One thumb rests outside the screen near bottom right. The display is an evenly illuminated PURE SATURATED GREEN tracking screen, solid #00ff00, with no reflections, no shapes, no writing, no interface or icons. Screen has flat straight parallel vertical and horizontal edges with gently rounded corners. Green area fills almost the entire phone display. This blank display will be replaced in post-production with the real camera preview, so keep its shape and camera position fixed. The mother simply presses once at the bottom center of the green screen with her right thumb at 2.5 seconds, then returns thumb to the edge. Left hand supports phone from the side, never covers screen. One phone, normal hands, no extra fingers, no body or face. Warm softly blurred oak tabletop and sage kitchen in background, cream flyer with coral and teal balloons lying on tabletop mostly concealed behind phone. Natural skin texture, warm daylight, very shallow depth of field outside phone. Locked camera, no pan, no zoom, no shake, no cuts, no music, no speech, a subtle fingertip tap only. Actual continuous filmed hand motion, not a still image.';\nasync function read(file)`);
script=script.replace("name.startsWith('v2-insert-')","name.startsWith('v2-insert-')||name.startsWith('v3-insert-')");
await fs.writeFile('scripts/generate-fridge-v3-camera.mjs',script);
