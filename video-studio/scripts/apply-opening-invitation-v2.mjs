import fs from 'node:fs/promises';
let s=await fs.readFile('src/mom-just-snap-it/SnapAnime.tsx','utf8');s=s.replace('import discoveryTracking from "./discovery-tracking.json";','import discoveryTracking from "./discovery-tracking.json";\nimport openingTracking from "./opening-tracking.json";');
s=s.replace('const q = track?.quad;','const q = track?.quad;\n  const opening = file === "opening-lettering-base.mp4" ? openingTracking[frame] : undefined;');
const marker='      {q && track.opacity > 0 ? (';
s=s.replace(marker,`      {opening?.matrix && opening.opacity > 0 ? (\n        <div style={{position:"absolute",width:1920,height:1080,left:(videoWidth-1920*mediaScale)*positionX,top:(videoHeight-1080*mediaScale)*positionY,transform:\x60scale(\x24{mediaScale})\x60,transformOrigin:"0 0",pointerEvents:"none"}}>\n          <div style={{position:"absolute",inset:0,clipPath:\x60path(evenodd, "\x24{opening.clip}")\x60}}>\n            <svg aria-hidden="true" width="400" height="500" viewBox="0 0 400 500" style={{position:"absolute",left:0,top:0,overflow:"visible",transform:\x60matrix3d(\x24{opening.matrix.join(",")})\x60,transformOrigin:"0 0"}} fill="#31565a" textAnchor="middle" fontFamily="Arial, sans-serif">\n              <text x="200" y="95" fontSize="62" fontWeight="700">Mia’s 8th</text>\n              <text x="200" y="163" fontSize="64" fontWeight="700">Birthday</text>\n              <text x="200" y="238" fontSize="36" fontWeight="700">SAT OCT 17, 2026</text>\n              <text x="200" y="303" fontSize="46" fontWeight="700">2–4 PM</text>\n              <text x="200" y="373" fontSize="40" fontWeight="700">MAPLE PARK</text>\n              <text x="200" y="432" fontSize="29" fontWeight="600">820 W 7th Street</text>\n              <text x="200" y="478" fontSize="28" fontWeight="600">Austin, TX</text>\n            </svg>\n          </div>\n        </div>\n      ) : null}\n`+marker);
s=s.replace(': "opening.mp4";',': "opening-lettering-base.mp4";');await fs.writeFile('src/mom-just-snap-it/SnapAnime.tsx',s);
let pkg=await fs.readFile('scripts/package-snap-anime-project.mjs','utf8');pkg=pkg.replace("'discovery-tracking.json']","'discovery-tracking.json','opening-tracking.json']");await fs.writeFile('scripts/package-snap-anime-project.mjs',pkg);
console.log('Opening details added as an editable, perspective-tracked shared layer.');
