import fs from 'node:fs/promises';import dotenv from 'dotenv';
dotenv.config({path:'../.env',quiet:true});dotenv.config({path:'../.env.local',override:true,quiet:true});
const data=await fs.readFile('projects/wedding-200-texts/final-audio.mp3');
const response=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent',{method:'POST',headers:{'x-goog-api-key':process.env.GEMINI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{inlineData:{mimeType:'audio/mpeg',data:data.toString('base64')}},{text:'Listen to this completed short advertisement audio. Transcribe all audible SPOKEN words exactly, without timestamps. Identify any repeated or cut-off words. Is dialogue natural and intelligible over music? Is the music instrumental and playful? Does music stop after the last dialogue, leaving a quiet ending? Report actual audio defects only. Concise JSON.'}]}],generationConfig:{temperature:0.1}}),signal:AbortSignal.timeout(60000)});
if(!response.ok)throw new Error('Review HTTP '+response.status);const j=await response.json();const t=j.candidates[0].content.parts.map(p=>p.text||'').join('\n');await fs.writeFile('projects/wedding-200-texts/final-audio-review.json',t);console.log(t);
