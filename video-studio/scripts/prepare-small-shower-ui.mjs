import fs from 'node:fs/promises';import {execFileSync} from 'node:child_process';
const pub='public/projects/small-shower',p='projects/small-shower';const m=JSON.parse(await fs.readFile(p+'/capture-timing.json','utf8'));
for(const [name,target] of [['chat',2.4],['card',1.8],['details',1.8],['share',2.2],['rsvp',2.2],['gift',1.6],['calendar',1.7],['directions',1.5]]){const len=m[name+'End']-m[name]-(name==='chat'?.25:0);execFileSync('ffmpeg',['-y','-hide_banner','-loglevel','error','-ss',String(m[name]),'-t',String(len),'-i',m.video,'-an','-vf',`setpts=PTS-STARTPTS,setpts=${target/len}*PTS,fps=30,setsar=1`,'-frames:v',String(Math.round(target*30)),'-c:v','libx264','-preset','fast','-crf','16','-pix_fmt','yuv420p',`${pub}/ui-${name}.mp4`],{stdio:'inherit'});}
let shared=await fs.readFile('src/small-shower/Shared.tsx','utf8');shared=shared.replace('style = {},','style = {},\n  trimBefore = 0,').replace('style?: React.CSSProperties;','style?: React.CSSProperties;\n  trimBefore?: number;').replace('    muted','    startFrom={trimBefore}\n    muted');await fs.writeFile('src/small-shower/Shared.tsx',shared);
let prod=await fs.readFile('src/small-shower/Product.tsx','utf8');prod=prod.replace('position={guest ? "75%" : "70%"}','position={guest ? "75%" : "70%"}\n          trimBefore={kind === "directions" ? 51 : 0}');await fs.writeFile('src/small-shower/Product.tsx',prod);
console.log('Clean app recordings prepared.');
