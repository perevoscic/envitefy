# Copy to signing.properties (ignored by Git). Paths resolve from android/.
# Use relative paths, or forward slashes with an escaped drive colon (C\:/...).
# Keep the real key and passwords private.
storeFile=.signing/envitefy-upload.p12
storePassword=REPLACE_LOCALLY
keyAlias=envitefy-upload
keyPassword=REPLACE_LOCALLY
